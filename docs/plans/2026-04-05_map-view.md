# Kartenansicht — QSOs auf OpenStreetMap

## Context
QSOs enthalten bereits Maidenhead-Locator (`locator`, `myLocator`) und die Konvertierung zu Lat/Lon ist in `src/utils/locator.ts` implementiert. Eine geografische Visualisierung fehlt bisher. Ziel: Neue Seite `/map` mit OpenStreetMap-Karte, QSOs als Marker, Filter nach Datum/Mode/Band.

## Annahmen
- Leaflet 1.9.x ist kompatibel mit Vite 7 + TypeScript 5.9
- CartoDB-Dark-Tiles (`basemaps.cartocdn.com`) bleiben öffentlich verfügbar (kein API-Key nötig)
- QSO-Anzahl mit Locator typischerweise <5.000 (passt in Speicher via Dexie `toArray()`)
- 4- und 6-stellige Maidenhead-Genauigkeit reicht für Marker-Platzierung
- Leaflet-CSS kollidiert nicht mit TailwindCSS 4 (`.leaflet-*` Prefix)
- Kein Echtzeit-Update nötig — Neuladen bei Filterwechsel genügt
- Raw Leaflet + Composable statt vue-leaflet (bessere Vue-3-Kompatibilität, folgt bestehendem Composable-Pattern)
- "About" wird in der mobilen Bottom-Navbar durch "Karte" ersetzt (Desktop-Sidebar behält alle 6 Items)
- Filter-State ist unabhängig von QsoHistoryView (verschiedene Nutzungskontexte)
- QSOs ohne Locator werden stillschweigend ausgeblendet, Anzahl wird im Status angezeigt

## Option A — Minimal/Schnell (S, ~4–6h)

**Ziel**: Schnellster Weg zu einer funktionierenden Karte mit Basis-Markern und einfachen Filtern.

**Vorgehen**:
1. `npm install leaflet && npm install -D @types/leaflet`
2. `src/composables/useLeafletMap.ts` — Leaflet-Map initialisieren, OSM-Tiles, `addCircleMarkers()`, `clearMarkers()`, `fitBounds()`, Cleanup in `onUnmounted`
3. `src/views/MapView.vue` — Full-Height-Karte, QSOs laden via `qsoRepository.getAll()`, Locator→LatLon filtern, CircleMarker setzen, Popup mit Callsign/Datum/Mode/Band/Distanz, Inline-Filter (Datum, Mode, Band), Statuszeile
4. `src/router/index.ts` — Route `/map` (lazy-loaded)
5. `src/components/common/AppLayout.vue` — Nav-Item "Karte" mit Karten-Icon
6. `src/i18n/locales/de.json` + `en.json` — Schlüssel für Karte/Filter/Status

**Vorteile**: Wenige Dateien, minimale Dependencies (~40kB gzip), schnell fertig
**Nachteile**: Kein Clustering (unübersichtlich ab ~200 QSOs), keine Dark-Mode-Tiles, keine Verbindungslinien, überlappende Marker bei gleichem Locator

## Option B — Vollständig/Skalierbar (M, ~8–12h) ← Empfohlen

**Ziel**: Feature-komplette Karte mit Marker-Clustering, Dark-Mode-Tiles, eigener Station, Verbindungslinien, Offline-Tile-Caching.

**Vorgehen**:
1. `npm install leaflet leaflet.markercluster && npm install -D @types/leaflet @types/leaflet.markercluster`
2. `src/types/map.ts` — `MapMarker` und `MapQsoSummary` Interfaces
3. `src/utils/mapHelpers.ts` — `groupQsosByLocator(qsos, ownLocator)` gruppiert QSOs nach Locator, berechnet Lat/Lon + Distanz/Bearing über bestehende Utils
4. `src/composables/useLeafletMap.ts` — Dynamic Import für Lazy Loading, zwei Tile-Layer (OSM light + CartoDB dark), automatischer Wechsel bei Theme-Änderung, `L.markerClusterGroup`, Methoden: `init()`, `setMarkers()`, `setOwnStation()`, `drawLines()`, `fitBounds()`, `destroy()`
5. `src/components/map/MapFilterBar.vue` — Kompakte Filter (Datum, Mode-Pills, Band-Pills), kollapsibel auf Mobile
6. `src/views/MapView.vue` — FilterBar oben, Karte darunter, Floating-Buttons ("Zentrieren", "Verbindungslinien"), Status-Leiste, Loading-State während Leaflet lädt, gruppierte Popups bei mehreren QSOs am selben Locator
7. Dark-Mode CSS-Overrides für Leaflet-Popups/Controls in `MapView.vue` (scoped)
8. `src/router/index.ts` — Route `/map`
9. `src/components/common/AppLayout.vue` — Nav-Item + mobile Bottom-Bar anpassen (About raus → Karte rein)
10. `src/i18n/locales/de.json` + `en.json` — Erweiterte Schlüssel (Karte, Filter, Verbindungslinien, Status)
11. `vite.config.ts` — Workbox `runtimeCaching` für OSM- und CartoDB-Tiles (CacheFirst, 500 Einträge, 30 Tage)

**Vorteile**: Clustering für große QSO-Mengen, Dark-Mode-Tiles passend zum App-Theme, Verbindungslinien (DX-Visualisierung), gruppierte Popups, Tile-Caching für Offline, Lazy Loading (kein Initial-Bundle-Impact)
**Nachteile**: Mehr Implementierungsaufwand, zwei zusätzliche Dependencies, CartoDB benötigt Internet beim ersten Laden

## Status
Umgesetzt: 2026-04-05 — Option B

Abweichungen:
- `@apply` in scoped Vue-Styles nicht möglich (Tailwind v4 braucht `@reference`). Ersetzt durch explizite CSS-Werte + `:global(.dark)` Selektoren.
- `buildPopupHtml()` nutzt kein `t()` (Popup-Texte hartcodiert als HTML), daher `t`-Parameter entfernt und direkt als `ownStationLabel: string` übergeben.
- `currentOwnCallsign`-Variable entfernt (war nicht lesend genutzt).

---

## Option C — Lightweight/Lazy (S/M, ~5–8h)

**Ziel**: Minimaler Bundle-Impact durch vollständig dynamisches Laden, farbcodierte CircleMarker nach Band, Dark-Mode-Tiles, aber ohne Clustering.

**Vorgehen**:
1. `npm install leaflet && npm install -D @types/leaflet`
2. `src/composables/useLeafletMap.ts` — Alles via `await import('leaflet')` in `async init()`, CircleMarker statt Pin-Icons (kein Asset-Problem), `BAND_COLORS`-Map für farbliche Kodierung, Light/Dark-Tile-Wechsel
3. `src/views/MapView.vue` — Loading-Spinner während Leaflet lädt, Inline-Filter, farbige CircleMarker mit Band-Legende, eigene Station als größerer Kreis, Auto-fitBounds, Statuszeile
4. Route, Navigation, i18n — wie Option A

**Vorteile**: Null Initial-Bundle-Impact, keine Icon-Asset-Probleme, Band-Farben als nützliche visuelle Kodierung, Dark-Mode-Tiles, nur eine Dependency
**Nachteile**: Kein Clustering, Ladelatenz beim ersten Besuch, keine Verbindungslinien, CircleMarker visuell schlichter

## Kritische Dateien

| Aktion | Datei | Option |
|--------|-------|--------|
| Neu | `src/views/MapView.vue` | A, B, C |
| Neu | `src/composables/useLeafletMap.ts` | A, B, C |
| Neu | `src/types/map.ts` | B |
| Neu | `src/utils/mapHelpers.ts` | B |
| Neu | `src/components/map/MapFilterBar.vue` | B |
| Ändern | `src/router/index.ts` | A, B, C |
| Ändern | `src/components/common/AppLayout.vue` | A, B, C |
| Ändern | `src/i18n/locales/de.json` | A, B, C |
| Ändern | `src/i18n/locales/en.json` | A, B, C |
| Ändern | `vite.config.ts` | B |

## Wiederzuverwendende Funktionen
- `locatorToLatLon()` — `src/utils/locator.ts`
- `calculateDistanceKm()` — `src/utils/locator.ts`
- `calculateBearing()`, `bearingToCompass()` — `src/utils/locator.ts`
- `qsoRepository.getAll(filters)` — `src/db/repositories/qsoRepository.ts`
- `settingsStore.ownLocator` — `src/stores/settingsStore.ts`
- `QSOFilters` Type — `src/types/qso.ts`
- Mode/Band-Konstanten aus `QsoFilters.vue` / `src/utils/frequency.ts`

## Verifikation
1. `/map` navigieren — MapView rendert ohne Console-Errors
2. QSOs mit Locator (JN47ao, JO31dc, FN20ar) anlegen → Marker an korrekten Positionen
3. Marker klicken → Popup zeigt Callsign, Datum, Mode, Band, Distanz
4. Eigenen Locator in Settings setzen → eigene Station wird markiert, Distanzen in Popups
5. Datumsfilter anwenden → nur passende QSOs auf der Karte
6. Mode/Band-Filter → nur passende QSOs
7. Dark Mode umschalten → Tile-Layer wechselt (Option B/C), Popups dunkel
8. 375px Breite → Karte füllt Bildschirm, Bottom-Nav enthält Karte
9. Locale auf Deutsch → alle Map-Labels auf Deutsch
10. Keine QSOs mit Locator → informativer Leer-Zustand
11. Zwei QSOs am selben Locator → ein Marker, Popup listet beide (Option B)
12. 50+ Marker, rauszoomen → Clustering mit Zähler-Badges (Option B)
